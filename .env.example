# -----------------------------------------------------------------------------
# Environment variables for TERAMOTO application
#
# INSTRUCTIONS:
# 1. Copy this file to a new file named .env in the root of the project.
#    cp .env.example .env
# 2. Fill in the values for each variable below.
# 3. IMPORTANT: Do NOT commit the .env file to your git repository.
#    The .gitignore file is already configured to ignore it.
# -----------------------------------------------------------------------------

# --- Firebase Configuration ---
# These values can be found in your Firebase project settings.
# Go to Project Settings > General > Your apps > Web app > SDK setup and configuration
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# --- Admin User Configuration ---
# The email address of the user who should have admin privileges in the app.
NEXT_PUBLIC_ADMIN_EMAIL=

# --- Google AI (Genkit) API Key ---
# This key is used for the AI Assistant features.
# You can generate a key from Google AI Studio.
GOOGLE_API_KEY=

# --- Google reCAPTCHA Enterprise Keys (Optional) ---
# Used to protect forms from bots. If you don't use it, leave them blank.
# Create keys here: https://cloud.google.com/recaptcha-enterprise/docs/create-key-website
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
